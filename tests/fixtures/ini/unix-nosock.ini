; The same http+unix install with no HTTP_ADDR, so nothing names the socket
; the health check would have to connect to: settings warns and the strict
; resolver dies.
[server]
PROTOCOL = http+unix

[database]
DB_TYPE = sqlite3
