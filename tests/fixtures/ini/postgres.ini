; PostgreSQL variant for the database-type tests: forgejo dump's zip is not a
; safe restore for it, so the script warns and asks for a native dump.
[server]
PROTOCOL = http
HTTP_ADDR = 127.0.0.1
HTTP_PORT = 3000

[database]
DB_TYPE = postgres
HOST = 127.0.0.1:5432
