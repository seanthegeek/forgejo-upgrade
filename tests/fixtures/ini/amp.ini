; A referenced value containing "&". Unquoted, bash's patsub_replacement turns
; an "&" in the replacement back into the reference, so this file is what
; proves ini_get quotes it.
[server]
HTTP_ADDR = /run/a&b/forgejo.sock
LOCAL_ROOT_URL = http://unix%(HTTP_ADDR)s/
